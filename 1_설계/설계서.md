# 1. 프로그램 설계서 (5점)

## 1. 프로젝트 주제
내 휴대폰의 성능 비교(벤치마크 점수로)
## 2. 핵심 기능 목록
(구현할 기능들을 순서대로 나열하세요.)
1.  휴대폰 기종 입력 시 어느 정도의 성능인지 보여줌(다른 기종과의 비교로)  
2.  그 기종의 특징등을 같이 보여줌
3.  점수 비교로 지금 휴대폰을 바꿀 때인지 아님 계속 쓸지 보여줌
4.  

## 3. 데이터 구조 설계
(프로그램에 필요한 핵심 변수, 배열을 설계하세요. 필요하면 구조체(struct)를 사용할 수도 있습니다.)

**방법 1: 배열 사용 (기본)**
```c
/char names[20][50];        // 최대 20개 기종 이름
int scores[20];            // 벤치마크 점수
char features[20][200];    // 특징 설명
int count = 0;             // 현재 저장된 기종 수

**방법 2: 구조체 사용 (선택)**
```c
// 휴대폰 정보를 저장할 구조체 정의
typedef struct {
    char name[50];      // 휴대폰 기종 이름
    int score;          // 벤치마크 점수 (예: AnTuTu)
    char features[200]; // 기종 특징
} Phone;

// 최대 20개 기종 저장 가능
Phone phoneList[20];
int phoneCount = 0; // 현재 저장된 기종 수
```
