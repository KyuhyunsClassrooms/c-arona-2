# 2. 알고리즘 (의사 코드) (10점)

## 1. `main` 함수의 전체 흐름
(프로젝트의 전체 작동 로직을 의사 코드로 작성하세요.)
시작
    미리 여러 휴대폰 기종 정보(이름, 점수, 특징)를 배열/구조체에 등록

    반복 (사용자가 '0'을 입력할 때까지):
        메뉴 출력
        1. 휴대폰 성능 조회
        0. 종료
        사용자 선택 입력

        만약 선택이 1이면:
            사용자에게 휴대폰 기종 이름 입력받기
            입력한 이름으로 등록된 목록에서 검색
            만약 해당 기종이 존재하면:
                성능 점수, 특징, 교체 추천 여부 출력
            존재하지 않으면:
                "해당 기종이 등록되어 있지 않습니다." 출력
        만약 선택이 0이면:
            반복 종료
반복 끝
종료

## 2. 핵심 기능 1:성능 비교 함수
```
(핵심 기능 1의 논리적 절차를 작성하세요.)

시작
    사용자로부터 조회할 휴대폰 기종 이름 입력받기

    반복문을 이용해 전체 등록된 기종(예: phoneList[ ]) 탐색
        만약 입력한 이름과 일치하는 기종이 있으면:
            1. 기종 이름 출력
            2. 벤치마크 점수 출력
            3. 특징 설명 출력
            4. 교체 추천 여부 판단:
                - 점수가 100만 이상이면 → "아직 짱짱함
                - 점수가 80만~99만이면 → "돈이 넘쳐나면 함 바꿔"
                - 점수가 80만 미만이면 → "이딴 것도 폰이냐?."
            5. 함수 종료

    일치하는 기종이 없으면:
        "이상한 힙스터 폰 쓰지 마라
종료

## 3. 핵심 기능 2: 폰 특징을 보여줌
```
(핵심 기능 2의 논리적 절차를 작성하세요.)
시작
    미리 여러 휴대폰 기종 정보(이름, 점수, 특징)를 구조체 배열에 등록

    반복 (사용자가 '0'을 입력할 때까지):
        메뉴 출력
        1. 휴대폰 성능 및 특징 조회
        0. 종료
        사용자 선택 입력

        만약 선택이 1이면:
            사용자에게 휴대폰 기종 이름 입력받기
            입력한 이름으로 등록된 목록에서 검색
            만약 해당 기종이 존재하면:
                성능 점수 출력
                특징 1~2개 출력
                교체 추천 여부 출력
            존재하지 않으면:
                "해당 기종이 등록되어 있지 않습니다." 출력
        만약 선택이 0이면:
            반복 종료
반복 끝
종료
